{
  "name": "portfolio-website",
  "version": "1.0.0",
  "description": "A basic portfolio website showcasing projects and skills.",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "build": "npm install && mkdir -p build && cp -r public/* build/ && cp -r views build/",
    "clean": "rimraf build",
    "prebuild": "npm run clean"
  },
  "dependencies": {
    "@netlify/functions": "^1.0.0",
    "ejs": "^3.1.9",
    "express": "^4.17.1",
    "serverless-http": "^3.1.1"
  },
  "devDependencies": {
    "fs-extra": "^11.3.0",
    "nodemon": "^2.0.15",
    "rimraf": "^3.0.2"
  },
  "author": "Bantu Nagarjuna",
  "license": "ISC"
}
